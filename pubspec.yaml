name: rate_link
description: "Exchange rate tracker app written in flutter"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  connectivity_plus: ^7.1.1
  dio: ^5.4.0
  fl_chart: ^0.68.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  hive_flutter: ^1.1.0
  intl: ^0.19.0
  path_provider: ^2.1.5
  share_plus: ^13.1.0

dev_dependencies:
  build_runner: ^2.4.9
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true
